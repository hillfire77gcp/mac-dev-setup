# {{ project_name }}

{{ project_description }}

## Installation

```bash
# From PyPI (once published)
pip install {{ project_slug }}

# From source
git clone https://github.com/{{ github_username }}/{{ project_slug }}.git
cd {{ project_slug }}
pip install -e ".[dev]"
```

## Usage

```bash
# Basic usage
{{ project_slug }} hello

# With custom name
{{ project_slug }} hello --name Alice

# Show version
{{ project_slug }} version

# Get help
{{ project_slug }} --help
```

## Development

```bash
# Install dependencies
uv sync --all-extras

# Run tests
just test

# Lint and format
just lint
just format

# Type check
just typecheck
```

## Project Structure

```
{{ project_slug }}/
├── src/
│   └── {{ project_slug }}/
│       ├── __init__.py
│       └── cli.py
├── tests/
│   └── test_cli.py
├── docs/
├── pyproject.toml
├── justfile
└── README.md
```

## License

{{ license }}

## Author

{{ author_name }} <{{ author_email }}>
